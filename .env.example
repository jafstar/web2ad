# Genstock — reusing the ezcontent Supabase project (gnbxwvzlnzromuawcswx),
# taken over as genstock's dedicated DB. Values below come from that
# project's API settings (Supabase dashboard > Project Settings > API).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Operator-owned generation keys — genstock is a hosted paid product, not
# BYOK like designpipe-app, so these live server-side only.
BFL_API_KEY=
GEMINI_API_KEY=

# Reusing ez-content's existing Cloudinary account for image storage.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Stripe — placeholders until the account/products are created (per
# tonight's decision: scaffold now, fill in when Stripe is set up).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_10=
STRIPE_PRICE_ID_20=

NEXT_PUBLIC_SITE_URL=http://localhost:3000
